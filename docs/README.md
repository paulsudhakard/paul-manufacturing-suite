# docs/

- `Architecture_v3.md`, `TechnicalDesignDocument_v1.md`,
  `ImplementationPlan_Sprints0-20.md` — the three frozen governing
  documents (copied here so the repo is self-contained).
- `ArchitectureDiagrams/` — reserved for exported/rendered versions of the
  ASCII diagrams already embedded in Architecture v3; not populated in
  Sprint 0.
- `ProductDefinitionAuthoringGuide.md` — the guide that makes adding
  product #3+ a documented process. Deliberately **not** written yet
  (Architecture v3 §20.4): authoring it now, before Rubber Stamp exists,
  would mean writing it from a single data point.
- `ExtensionSDK/` — public docs for future third-party developers. 🔴
  deferred per Architecture v3 §17.3 Tier 3 — reserved, not populated.
