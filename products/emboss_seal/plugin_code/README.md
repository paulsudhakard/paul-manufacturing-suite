# products/emboss_seal/plugin_code/

Product-specific logic implementing the `ProductPlugin` contract
(`validate()`, `analyze()`, `repair()`, `generate_outputs()`,
`generate_preview()` — TDD §7).

**Arrives:** Sprint 12 (core hooks), Sprint 13 (Male/Female generator),
Sprint 14 (preview renderer), Sprint 18 (report builder).
