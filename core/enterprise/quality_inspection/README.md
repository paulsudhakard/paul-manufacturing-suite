# core/enterprise/quality_inspection/

`QualityInspectionEngine`, `MachineCompatibilityChecker`,
`MaterialCompatibilityChecker` — the pre-export gate reusing the Validation
Engine's exact check implementation (FR-8, Architecture v3 §14.4, TDD §2.4).

**Arrives:** Sprint 17.
