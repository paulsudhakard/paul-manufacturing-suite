# core/enterprise/operators/

Operator accounts and roles (`admin` / `operator` / `viewer`, TDD §30).

**Arrives:** proposed Sprint 22 ("Gaps" section of the Implementation Plan).
Sprint 8 implements only a narrow, explicitly-flagged `admin`-only stopgap
for a single write endpoint — not the full model described here.
