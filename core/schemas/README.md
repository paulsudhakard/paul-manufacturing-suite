# core/schemas/

Formal JSON Schema versions of TDD §5–16 (`neutral_geometry.schema.json`,
`product_definition.schema.json`, `workflow_definition.schema.json`,
`job.schema.json`, etc. — TDD §34).

**Arrives:** `neutral_geometry.schema.json` in Sprint 4; each further schema
alongside the sprint that implements the engine it validates input for.
