# core/engines/repair/

`repair_actions/*` (close open curves, join endpoints, remove duplicates,
remove tiny islands, simplify nodes), the bounded loop orchestrator
(default max 3 attempts), and the similarity scorer (TDD §2.2).

**Arrives:** Sprint 10.
