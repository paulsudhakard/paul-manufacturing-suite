# core/engines/rule_engine/

`RuleCategoryLoader`, `RuleResolver` — resolves manufacturing rule
thresholds via product → material → machine → default (Architecture v3
§12.3, TDD §9).

**Arrives:** Sprint 7.
