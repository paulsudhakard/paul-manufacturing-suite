# core/engines/geometry/

`CurveUtils`, `ShapeGraph`, `OffsetEngine` — curve ops, connectivity graph,
parametric offset. Product-blind: operates only on Neutral Geometry +
numeric parameters (Architecture v3 §4, §11).

**Arrives:** Sprint 5.
