# core/engines/notification/

`NotificationEngine.emit()` — event dispatch to configurable channels;
`in_app` is the only v1 build target (FR-13, TDD §16).

**Arrives:** proposed Sprint 23 (see Implementation Plan, "Gaps in the
Given 20-Sprint List") — not in scope for Sprints 0–20 as currently
approved.
