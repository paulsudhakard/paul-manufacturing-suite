# core/engines/ai_recommendation/

`AIRecommendationEngine.recommend()` — rule-based v1 behind an API shaped
for a future model-backed version; delegates product-specific
interpretation to the `ProductPlugin` (Architecture v3 §13).

**Arrives:** not scheduled in Sprints 0–20 (no sprint currently claims it —
flag before scheduling Sprint 21+ if this is needed sooner).
