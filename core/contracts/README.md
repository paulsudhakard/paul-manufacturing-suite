# core/contracts/

The load-bearing interfaces: `product_definition_contract.*`,
`plugin_lifecycle_contract.*`, `extension_sdk_contract.*` (Architecture v3
§3/§8), plus `error_model.md` and `exception_hierarchy.md` (TDD §34).

**Arrives:** `product_definition_contract` and `plugin_lifecycle_contract`
take real shape starting Sprint 11–12 as the Workflow/Plugin Engines are
built; `error_model.md`/`exception_hierarchy.md` document Sprint 1's real,
throwable exception hierarchy (TDD §18) once it exists.
