# core/data/

`models.*` — Job, Customer, Operator, RuleVersion, MachineProfile,
MaterialProfile, ProductDefinitionVersion, WorkflowInstance (Architecture
v3 §3).

**Arrives:** first real persisted model (`Job`, `WorkflowInstance`) in
Sprint 11, which is also the first sprint requiring a real database.
