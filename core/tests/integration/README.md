# core/tests/integration/

Adapter↔Core contract tests, plugin conformance integration, cross-engine
integration (TDD §38).

**Arrives:** Sprint 1 (`test_core_skeleton_startup.py` — full config/log/
exception-hierarchy self-test). Adapter↔Core health-check round trip
added in Sprint 3.
