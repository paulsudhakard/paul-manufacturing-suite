# core/tests/regression/baselines/

Golden-output snapshots (`AnalysisResult` + `ValidationReport` +
`RepairResult`) per curated test document — government seal, church logo,
signature, corrupted file, and their Rubber Stamp equivalents once that
plugin exists (TDD §39).

**Arrives:** Sprint 6 onward.
