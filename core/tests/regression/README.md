# core/tests/regression/

Golden-output baselines, cross-plugin regression, determinism checks
(TDD §39). See `baselines/` for the checked-in golden outputs themselves.

**Arrives:** Sprint 6 onward (first curated-test-document baselines);
cross-plugin regression specifically from Sprint 20.
